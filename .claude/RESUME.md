# RESUME — micahjonesconsulting (2026-09-01 PM)

## HARNESS Pass-61: arc-shape gate LIVE — full policy in `MODEL_ROUTING.md` §6
Audit: 9 of 320 Fable turns were taste calls, 311 execution. DIRECT (Fable) now ends by
committing a brief (`.claude/briefs/README.md`); EXECUTE runs it verbatim; JUDGE returns 3x.
Live+tested global gates: statusline `5h NN%`; tier-burn-monitor (12 Fable / 30 Opus / 75+90%,
silent on Sonnet); secret-literal-gate DENIES token literals (proven live); agent-model-gate
WARNS (advisory, never blocks) when an Agent call names no model. Card §4 corrected: 31 events.
TWO SESSIONS ran this tree tonight — two budgets, one 5h window. The other's Pass-55 superseded
my /playbook rebuild; prod serves Pass-55. Fable `page-cro` ruling on live /playbook pending.

## UNPUSHED: Pass-57/58 (88bbafe), Pass-59 (26e3ed7), Pass-60 (7f4774f)
Live prod = Pass-53..56c (musrbwwz4). Prod deploys are CLI, not git push.
Pass-60 (detail in the commit): author study SPLIT into /work/content-engine + /work/rfp-engine;
old page RETIRED (308 -> rfp-engine) since it carried the sector wording and the same 8K->290K
figures. "equity" now zero rendered. Guardicore: $1.2M average deal size SUPERSEDES the $150K
delta; trillions line RESTORED; both ledgered with provenance. Ordani: user counts gone
site-wide -> "active paying users", beta; a live "HIPAA-grade" in llms.txt fixed.

## NEXT: page-opening redesign — DIRECTION PROPOSED, AWAITING "go"
Operator: services/work/contact openings "underwhelming… premium without screaming AI". Fable:
/services = cut the header, two full-height columns split by one rule; /work = espresso "lot 01"
auction-catalogue opening ($80M at hero scale, Tel Aviv photo as exhibit, provenance line),
hand-set order via frontmatter `order`; /book = espresso timetable slot grid. Prerequisite:
interior pages first-paint TERRACOTTA then cross-fade to bone (globals.css ~1334) —
server-render the opening world first. /work sorts author studies above Akamai.

## OPEN OPERATOR QUESTIONS
1. Photo rights: 429 images = 342 Pexels + 74 lemandjune + Getty + AdobeStock _Preview
   (watermarked). Only lemandjune matches live ordani-work.jpg. Owned? Stock is banned.
2. About: advised AGAINST the Jerusalem photo (political read; sunglasses hide the eyes).
3. Tel Aviv photo carries an Instagram location + avatar sticker to remove.
4. Ordani: heading "encryption at the row level" vs body "row-level security policies".
5. SECRET ROTATION overdue (Resend, ICS, Stripe test). 6. Stripe go-live. 7. tsx em-dash debt:
6 files / 13 over cap. 8. Wire `page-cro` into /premium audit as a Fable leg before ship.
9. Settings prune: ~21 of 460 allow entries redundant; audit found ZERO credentials.

## Gotchas
- Windows heredocs mangle backslashes: write regex-bearing TS with Edit, not python.
- Python writes CRLF -> prettier --write touched TS/TSX before commit.
- Secrets never inline (gate enforces). Ordani never names vendors. HIPAA-compliant, not -grade.
