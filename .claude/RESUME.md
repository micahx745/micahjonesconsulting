# RESUME — micahjonesconsulting (2026-09-11)

## READ FIRST

**Dark rebuild REJECTED; the LIVE site is being evolved.** Branch `design/live-evolve`
(worktree `.claude/worktrees/p106-live`), 90 commits ahead of `main`, merges clean.
**`pnpm build` fails HERE only** (Turbopack font error; Vercel is fine): use
`npx next build --webpack`. **Workflow ignores `CLAUDE_CODE_SUBAGENT_MODEL`**: name `model:`.

## Operator 2026-09-11, rulings (verbatim where quoted)

Neuton tag: "for neuton 2025". Postmates + Neuton stories: "add it as other case studies
and or work". Rename "Frontier AI engineering": "Yes". Name Guardicore in the proof card:
"Yes". /services: consolidate; the areas "describing the work that takes place within the
4 shapes"; "premium looking pricing boxes" for shapes AND packages, each listing
"everythging you get"; packages cover ONE area, "people have to pick which one on the
page". Home: drop "$2,500 · two weeks"; Audit becomes a box; "load bearing cannot be used
anywhere". Receipts: cut "every line below is real", the revenue line, exit dates, the
"stated on this site" row; bring back the circled $20M that climbs; premium exit record.
Ordani section: redesign, mobile pics "painful". "do the things that you can do without me."

## Plan

110 no-decision: a11y (doors, Ordani submit, focus ring + accent hovers, lede link, static
lint) + copy rulings. 111 pricing boxes on /services + home Audit. 112 receipts + Ordani.
113 Postmates + Neuton case studies. Design direction: workflow `design-direction-110`.
FLAG before merge: new service commitments; the Hennessy anecdote; count-up vs the
no-second-figure rule; Ordani GSAP pin.

## Done / OPEN

Pass-109 `7da7f11` pushed, preview green, Astra: overwhelming RESOLVED. OPEN: merge to
`main` (operator) · push each new pass (ask) · Stripe live $500 test.

## Traps

push to main auto-deploys · capture VIEWPORT, settle first · `python -P` · Git Bash: set
`MSYS_NO_PATHCONV=1` for "/" args · never read an exit code through a pipe · PS5.1 mangles
quotes, `git commit -F` · MORE THAN ONE world renders here: no fixed text colour, no opacity
on text, no `--cw-accent` under text · run `scripts/axe-worlds.mjs` before any colour commit.
