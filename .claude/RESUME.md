# RESUME — micahjonesconsulting (2026-09-21 night, Pass-128c section 7)

## READ FIRST
Kickoff: `.planning/handoff/NEXT-SESSION-KICKOFF.md` (step 1 in flight). Landing exemplar = SEPARATE chat.
Branch `design/live-evolve` (p106-live). Build: `node .planning/exec/prepush-gates.mjs` (#47). Push to main deploys.
Routing: `.claude/AI_ROUTING.md`. Write guard live (#49, #50). preview_start reads MAIN's launch.json: `prod-p124`.

## LIVE: main = e091a16, dpl_Bk18zCfBqPb2DjTrkozL2dBs7git, all three domains (Pass-126)
Revert: promote `dpl_2mGqgwPQ1BwZCHpoi4wek2dupiwH`. A push needs his words.

## IN FLIGHT: Pass-128c section 7 (p124-cuts, source `099c07a`, uncommitted: pkg/lock/2 probe scripts)
Sol: FAIL items 5 (drops 49 > 45) and 9 (Chrome would not launch in its sandbox). Main session: pnpm remove done;
rebuild PREPUSH pass; exact CLS 0.0193/0.00665; card1 local 0 failures; eyes on 2 sheets + 20 shots, no regression.
Item 5: the Audit heading's 0.55 s fade costs ~14 drops (A/B fix-ab/attrib: 50 vs 36 with the fade off); the <=45
came from an arm with no entrance. Live -> fix: drops 74 -> 49, frames over 33 ms 45 -> 2, p95 33.4 -> 16.8 ms.
Running: will-change A/B (fix-ab/willchange), DeepSeek (`reviews/DSPRO-128C-EVIDENCE.md`), Astra
(`reviews/ASTRA-128C-QUALITY.md`), Fable (one call). Then popup: reads by name, the fade option, ASK before a Vercel
preview for his phone. Ship only on his push words.

## FINDINGS FOR NEXT ARCS
128d premise WRONG: below the Audit the fix fires no link colour fades (color 15 vs 509; only bone changed the ink).
Remaining lower-page cost: exits scoreboard font-size transitions, How I work steps. Pre-existing: NEUTON.AI row
blank in normal-390 Ordani shot (exits items).

## NEXT
Doors build (127c) after 128c merges · exits dead swipes, exits title, Neuton "Undisclosed".

## PARKED BY HIM (LESSONS #3 "THE NINIAAZZOPARDI FLOW")
Exemplar builds it, this site reuses it; brand row later. Note: `.planning/handoff/NOTE-TO-LANDING-EXEMPLAR.md`.

## QUEUE
HELD /full-time · /about voice · Ordani "practice management" vs "CRM" · open facts · Guardicore LCP p75 · K4/K5
race · dead CSS · DeepSeek key rotation ("Not yet, I'll do it").

## Budget (~21:05 PDT): 5h 15%, weekly all 69%, Fable 73%; reset 09-26 01:00.

## Traps
explicit pathspecs (#23) · executors: no non-ASCII (#46), no bash, no pnpm on Sol's PATH · pre-flight brief values
in Sol's shell (#52) · a `cd` moves the session's primary dir: use `git -C` and full paths.
