# RESUME — micahjonesconsulting (2026-09-20)

## READ FIRST
Pass-123 ship gate (Opus 5 main). TIERS (operator 09-20): Claude week reset —
5h 1%, week 7%, Fable 5% at 10:30. GLM is 94% for the week, resets ~09-22, so
it is OUT: grunt = Sonnet legs (everything needing Chrome) and Sol via
`codex-exec.ps1 -Task` (file+build only, its sandbox has no Chrome); gates =
Opus/Fable/Astra. get_usage before every fan-out. Executors: pnpm may be off
PATH -> C:/Users/micah/AppData/Roaming/npm/pnpm.cmd; serve via
`node node_modules/next/dist/bin/next start -p 3236`; one .next at a time;
NEVER elementHandle.screenshot() (it scrolls and makes identical frames).

## LIVE: 3809f5e dpl_9fPDmK1W62BqCj9TriRSG6dJVkwp
Pass-123a+b+c+d. card1-123.sh 0 failures; production CLS 0.00000 @390 /
0.00714 @1440. Revert: promote dpl_HNtW9Ha (68a8dc3).

## BRANCH, UNPUSHED: the study bands, through ceb1c09
Stages 1-4, the operator's ticked cuts, the judge-gate answers, and now the
judge FIX ROUND (ceb1c09, committed 09-20): poster lead's last three words in
.cs-title__nb so 390 never breaks after a bare "a"; "Name protected" spacing
under the context line; .cs-num--comma 0.16em -> 0.12em. Fix round verified by
.planning/exec/band123-fixround-rerun2.log — 141 PASS, 1 FAIL that is the
K5@390 harness scrollIntoView-vs-Lenis race, not the page. Ink gap now 30-31px
@1440 / 13-13.5px @390 (was 39-40 against an expected 12-32). pnpm build green
at ceb1c09 (.planning/exec/build-p123-commitgate.log), prettier clean.
Earlier evidence: CLS 0.00000 all five studies both widths; LH mobile median
3375.8ms after vs 3389.8 before; route JS +1363B (limit 6144).

## IN FLIGHT
Workflow wf_4be9db6b-461 (4 Sonnet legs): recapture study-final at 390/1440
POST-fix (the 09-19 21:05 sheets predate ceb1c09) + three read-only audits —
ledger conformance, ticked-cuts conformance, adversarial code review of
3809f5e..HEAD. Pre-fix captures backed up to study-final-prefix/.
THEN: Micah sees the fresh before/final sheets + the live build in the pane
(`prod-p106`), then his dated push approval, then CARD 1.

## Queue after that
1. Home hero shift: 0.0045 of the 1440 CLS is h1.cw-h1 / p.cw-sub at load,
   pre-existing. Own small pass.
2. /work/guardicore mobile LCP ~3.4s (text LCP, p.cs-band__dek), pre-existing.
   Speed Insights field data was the agreed judge (Pass-120).
3. K5@390 harness race (background task filed).
4. HandCircle.tsx delete: ask him. Kickoff items 1 and 2 are DONE (the three
   small fixes landed in Pass-123a-d; RFP provenance is ledger rows at
   LESSONS #3 lines 785-799).

## Traps
#32 ledger before a leg · #33 every spelling · #34 no grep -iF · #35 no parked
copy · #36 marker leads the FINAL message · #37 no gaming a check · #38 field
consumers · #39 scope first · #40 reveals never empty · #41 scroll CLS · #42
assert the toolchain, prove liveness · #43 a scripted edit proves what landed ·
#23 recurrence: while a leg runs, commit only files outside its scope · #19
accent while scrolling · pathspec commits.
