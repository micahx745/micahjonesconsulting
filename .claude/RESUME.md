# RESUME — micahjonesconsulting (2026-09-01 PM)

## HARNESS Pass-61: arc-shape gate LIVE — full policy `MODEL_ROUTING.md` §6
Audit: 9 of 320 Fable turns were taste calls, 311 execution. DIRECT (Fable) now ends by
committing a brief (`.claude/briefs/README.md`); EXECUTE runs it verbatim; JUDGE returns 3x.
Global gates, all tested: statusline `5h NN%`; tier-burn-monitor (12 Fable / 30 Opus / 75+90%,
silent on Sonnet); secret-literal-gate DENIES token literals (proven live); agent-model-gate
WARNS (advisory) on unnamed Agent model; concurrent-session-guard (SessionStart). Card §4
corrected: 31 hook events exist. TWO SESSIONS ran this tree tonight, two budgets on one 5h
window; three collisions -> LESSONS #12.

## Pass-61 /playbook conversion pass (57971e2) — brief executed, build green
First brief under the new shape: `.claude/briefs/pass-61-playbook-cro.md` (Fable ruled, Opus
executed; six copy edits + share card, all verified in rendered HTML). Email capture 76/100
Moderate; **$99 purchase 68/100 LOW, NOT ready to flip** — purchase-phase layout is HELD in
brief §8 behind 3 gates: prod RESEND_API_KEY probe, Stripe account split, one live-mode
buy+refund with a real card.
**PROD STILL SERVES THE RETIRED "hundreds of birth workers" CLAIM.** HEAD has the fix,
unshipped. Largest trust correction available, zero engineering cost.

## UNPUSHED: 88bbafe, 26e3ed7, 7f4774f, b27b1a9, 1a8f6e3, 77c5359, 57971e2
Live prod = Pass-53..56c (musrbwwz4). Prod deploys are CLI, not git push.
Pass-60 (7f4774f, detail in the commit): author study split into content-engine + rfp-engine,
old page retired 308. Ledger moves that must not be reverted: Guardicore $1.2M average deal
size SUPERSEDES the $150K delta; Ordani user counts gone site-wide -> "active paying users".

## NEXT: page-opening redesign — DIRECTION PROPOSED, AWAITING "go"
Operator: services/work/contact openings "underwhelming… premium without screaming AI". Fable:
/services = cut the header, two full-height columns split by one rule; /work = espresso "lot 01"
auction-catalogue opening ($80M at hero scale, Tel Aviv photo as exhibit, provenance line),
order via frontmatter `order`; /book = espresso timetable slot grid. Opening world now hoisted
into <head> (77c5359). /work sorts author studies above Akamai.

## OPEN OPERATOR QUESTIONS
1. DEPLOY 57..61 (kills the retired prod claim). 2. Ship month for the spec card. 3. Portrait
for § 0.1 + the share card (stock is banned). 4. Real early-reader quotes, attributed only.
5. "$149 after" needs a real raise date or the anchor comes off. 6. Price lock for the list?
7. Photo rights: 429 images = 342 Pexels + 74 lemandjune + Getty + AdobeStock _Preview
(watermarked); only lemandjune matches live ordani-work.jpg. 8. Advised AGAINST the Jerusalem
photo; Tel Aviv photo still carries an Instagram sticker. 9. Ordani heading "encryption at the
row level" vs body "row-level security policies". 10. SECRET ROTATION overdue (Resend, ICS,
Stripe test). 11. Stripe go-live. 12. tsx em-dash debt: 6 files / 13 over cap. 13. Wire
`page-cro` into /premium audit. 14. Settings prune: ~21 of 460 redundant, ZERO credentials.

## Gotchas
- Windows heredocs mangle backslashes: write regex-bearing TS/JS with Edit, not python.
- Python writes CRLF -> prettier --write before commit. Screenshots return black below the
  fold on this machine; verify layout by computed geometry instead.
- Secrets never inline (gate enforces). Ordani never names vendors. HIPAA-compliant, not -grade.
