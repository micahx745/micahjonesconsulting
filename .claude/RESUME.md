# RESUME — micahjonesconsulting (2026-09-11)

## READ FIRST

**Dark rebuild REJECTED; the LIVE site is being evolved.** Branch `design/live-evolve`
(worktree `.claude/worktrees/p106-live`), ahead of `main`, merges clean.
**`pnpm build` fails HERE only** (Turbopack font error; Vercel is fine): use
`npx next build --webpack`. **Workflow ignores `CLAUDE_CODE_SUBAGENT_MODEL`**: name `model:`.

## Operator 2026-09-11 (verbatim where quoted)

Neuton "2025". Postmates + Neuton become case studies. Rename "Frontier AI engineering".
Name Guardicore in the proof card. /services: fold the three areas into the four shapes;
"premium looking pricing boxes" for shapes AND packages, listing "everythging you get";
packages cover ONE area, "people have to pick which one on the page". Home: drop "$2,500 ·
two weeks"; Audit becomes a box; "load bearing cannot be used anywhere". Receipts: cut the
framing lines, dates and the floor row; bring back the circled $20M that climbs; premium
exits. Ordani: redesign, mobile pics "painful". "make sure you are using the harness using
other AI models."

## In flight

- GLM 5.3: Pass-110 (brief `.claude/briefs/pass-110-a11y-copy.md`, package `9087aca`), report
  in `.planning/exec/glm110-report.txt`. It does NOT commit: check its diff, re-run the gates.
- Sol `gpt-5.6-sol`: Postmates + Neuton drafts, read-only, to
  `.planning/reviews/SOL-110-CASE-STUDIES-DRAFT.md`. Check every sentence against LESSONS #3.
- Workflow `design-direction-110` (Claude): references + direction for pricing boxes,
  receipts, Ordani. It feeds the Pass-111/112 briefs for GLM. Astra judges the result.

## OPEN

Merge to `main` (operator) · push each pass (ask) · Stripe live $500 test · FLAG before merge:
new service commitments, the Hennessy anecdote, count-up vs the no-second-figure rule.

## Traps

push to main auto-deploys · capture VIEWPORT, settle first · `python -P` · Git Bash: set
`MSYS_NO_PATHCONV=1` for "/" args · never read an exit code through a pipe · GLM `-Batch`: a
pointer prompt only, no quotes, no dashes · MORE THAN ONE world renders here: no fixed text
colour, no opacity on text, no `--cw-accent` under text.
