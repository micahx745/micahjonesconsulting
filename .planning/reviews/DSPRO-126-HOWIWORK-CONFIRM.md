VERDICT: SHOW HIM AFTER FIXES — strong redesign and better than BEFORE, but the main CTA is buried and one locked line reads like placeholder copy.

COPY: FLAGGED
- Scope: reads well.
- Plan: reads well except "Something named ships in month one." — the subject of "named" is unclear, so it reads as unfinished/AI placeholder.
- Build: reads well.
- Stay: reads well.

1. Home and /services, 1440 — "See the work" sits inline on Stay’s last line, so the section’s only forward action reads as another sentence inside the handoff copy rather than a destination. Fix: move it to its own line below Stay and give it clear CTA styling using the existing type/palette, with no word change.
2. Home, 390 — the small alternating indents are too subtle to register as a deliberate stair-step; the left edge can look accidentally ragged. Fix: increase the indent difference or align each step’s copper rule/headline so the offset is clearly intentional.
3. /Services, both widths — the two-line note under the steps can read as a fifth step unless it is visually separated from Stay. Fix: set it as a note with more separation, lighter Hanken, and no copper rule.

Best: the uneven editorial layout and copper reveal rules make the four steps feel like a real sequence rather than another spec sheet.