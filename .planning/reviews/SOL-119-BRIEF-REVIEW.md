1. **PREMISE-WRONG** — Reduced-motion behavior is not unchanged: today `gsap.matchMedia()` maintains live no-preference/reduce contexts (`components/color-worlds/SplitReveal.tsx:67-106`), while §3.1 performs one `.matches` check and returns (`.claude/briefs/pass-119-gsap-after-load.md:46-48`). Consequence: changing the OS preference after mount will neither revert an active split nor initialize one when motion becomes allowed.

2. **RISK** — R1 cannot distinguish baseline from the proposed behavior: “3s after load, a GSAP chunk has been requested” is true when it was requested before load too, despite the claimed baseline difference (`pass-119-gsap-after-load.md:94-102`); additionally, a `PerformanceResourceTiming` entry has metadata, not response content. Consequence: specify `GSAP absent through load, then requested afterward`, with request timing plus separately fetched/build-inspected bytes.

3. **RISK** — R2’s “no transform” is ambiguous because the unchanged tween ends at `yPercent: 0` without `clearProps` (`SplitReveal.tsx:82-96`), so GSAP may retain an identity inline/computed transform. Consequence: test opacity and identity/zero translation rather than literal `transform === "none"`.

4. **RISK** — `reveal119.mjs` remains underspecified: “at 1440” omits viewport height, R4 needs a preinstalled `DOMContentLoaded` listener to avoid racing `load`, and R1/R2 have the ambiguities above (`pass-119-gsap-after-load.md:94-102`). Consequence: `chunks119.mjs` is specific enough to write, but `reveal119.mjs` needs explicit 1440×900 setup, event instrumentation, and assertions before execution.

5. **RISK** — “home median performance >=95” is not established by `/services`: home’s recorded simulated score is 94 with LCP 3121ms (`.planning/qa/pass-118/prod/home-simulate-1.json:32-40,3086`), while the routes differ in content and even have a services-only chunk; §5 merely infers the threshold from 97 (`pass-119-gsap-after-load.md:103-105`). Consequence: retain “home LCP improves” and the services control as gates, but treat ≥95 as a target rather than a correctness failure.

6. **RISK** — The Lighthouse line is not runnable literally in Git Bash because `<url>` and `<file>` are shell-redirection syntax (`pass-119-gsap-after-load.md:80-82`). Consequence: enumerate the six concrete commands or explicitly label the line as a substitution template.

7. **PREMISE-OK** — Removing SplitReveal’s four static imports should remove GSAP from `/`’s initial graph: `/` imports only `SplitReveal` (`app/(foyer)/page.tsx:71`), `TitleCard` is imported only by the theater work route (`app/(theater)/work/[slug]/page.tsx:22`), and root/foyer layouts import neither (`app/layout.tsx:15-21`; `app/(foyer)/layout.tsx:22-26`). Consequence: TitleCard or shared-chunk formation does not itself make that chunk initial for `/`, though the §5.3 built-output check remains the necessary proof.

8. **PREMISE-OK** — All dynamic paths are valid for installed GSAP 3.15.0: the export map resolves `gsap` to `index.js` and subpaths to `*.js` (`node_modules/gsap/package.json:3,10-29`); GSAP exports named and default (`node_modules/gsap/index.js:1-6`), as do SplitText and ScrollTrigger (`node_modules/gsap/SplitText.js:333`; `node_modules/gsap/ScrollTrigger.js:921,2703`). Consequence: destructuring the named `gsap`, `SplitText`, and `ScrollTrigger` exports is valid.

9. **PREMISE-OK** — SplitText is present in the ordinary installed package and governed by its standard no-charge license (`node_modules/gsap/package.json:66`; `node_modules/gsap/SplitText.js:1-6`). Consequence: no Club GSAP package, token, or separate licensed artifact is required.

10. **PREMISE-OK** — The quarantine gate deliberately detects dynamic imports but skips both allowlisted owners (`scripts/gsap-quarantine-gate.mjs:28-39,80-95,145-148`). Consequence: the proposed imports inside `SplitReveal.tsx` pass both normal and self-test modes.

11. **PREMISE-OK** — `top <= innerHeight*N/100` matches ScrollTrigger’s `top N%` crossing calculation (`node_modules/gsap/ScrollTrigger.js:754-773`), and no home title begins in the first viewport: the first follows a `min-height:100vh` hero plus section padding (`app/(foyer)/page.tsx:115-116,156-165`; `app/globals.css:1824-1830,2458-2461,5091-5096`). Consequence: at 390 and 1440, normal scroll-zero loads do not turn a currently animating first-viewport title static.

12. **PREMISE-OK** — Explicitly killing the tween/ScrollTrigger before `split.revert()` is complete; tween kill already kills its attached trigger (`node_modules/gsap/gsap-core.js:3512-3520`) and SplitText revert disconnects observers and restores original DOM (`node_modules/gsap/SplitText.js:294-310`). Consequence: with the required effect-local unmounted flag and shared promise, Strict Mode double effects and the `ViewTransition` wrapper (`app/layout.tsx:145-148`) are safe.

13. **PREMISE-OK** — The local before/after Lighthouse runs use the same webpack build, server, CLI, Chrome, routes, and three-run protocol (`pass-119-gsap-after-load.md:74-83,91-105`). Consequence: their relative medians are comparable to each other, though not directly to the production absolute scores.