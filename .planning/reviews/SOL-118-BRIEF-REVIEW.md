1. **PREMISE-WRONG** — The brief calls 0.290 “CLS” (`pass-118-tuned-font-fallbacks.md:15,115-117,138-140`), but the cited review says stored/printed standards-filtered CLS is 0 and 0.290 is an all-shifts lab estimate (`PERF-118A-FINDINGS.md:17-22`; `SOL-118A-READ.md:5-7`). Consequence: verification may retain the stricter metric, but must not report it as standards CLS.

2. **PREMISE-WRONG** — “Nothing else in `lib/fonts.ts` changes” (`pass-118-tuned-font-fallbacks.md:31-34`) would leave its warning claiming `adjustFontFallback: true` injects the CLS-neutralizing face (`lib/fonts.ts:9-11`) immediately above settings changed to false (`lib/fonts.ts:35,53`). Consequence: the brief must permit correcting that now-false comment.

3. **RISK** — The brief states definitively that the generated Arial faces cause the wrapping (`pass-118-tuned-font-fallbacks.md:15-20`), while the evidence calls that only “likely” and not isolated (`PERF-118A-FINDINGS.md:80-83`; `SOL-118A-READ.md:21`). Consequence: the required old-metric bite (`pass-118-tuned-font-fallbacks.md:100-102`) is a genuine premise gate, not merely ceremonial.

4. **RISK** — The tuner specifies widths but no heights except the indirectly cited 412×823 probe, and does not define mobile/touch state for 390/768/1440, `innerHeight` versus `visualViewport.height`, boundary treatment, or CSS-pixel interpretation (`pass-118-tuned-font-fallbacks.md:74-79`; `PERF-118A-FINDINGS.md:5-6`). Consequence: two faithful executors can sample different “first viewport” elements.

5. **RISK** — “Own non-empty text,” “visible (box >2px),” Range scope, rounded line tops, box-height source, and whether the REAL or union membership controls comparison are undefined (`pass-118-tuned-font-fallbacks.md:77-85,97-99`); the hero contains nested text spans (`components/color-worlds/Hero.tsx:173-183,200-208`). Consequence: the tuner can miss or double-count the actual wrapping container.

6. **RISK** — Candidate lifecycle and selection ties are unspecified: replace versus accumulate the `"Cand"` face, unique family per S, post-load layout settling, equal longest runs, midpoint half-steps, and equal “best S” values (`pass-118-tuned-font-fallbacks.md:80-91`). Consequence: stale font-face caching or tie choices can change `chosen`.

7. **RISK** — `--after` and `--compare` do not say whether bite/search/verify also run, while selector-path construction and cross-build identity are undefined (`pass-118-tuned-font-fallbacks.md:92-96,120-143`); the font classes sit on `<html>` (`app/layout.tsx:140-145`) and their hashes derive from generated CSS (`next-font-loader/index.js:103-105`). Consequence: artifacts may be overwritten or unchanged geometry may be reported as element additions/removals.

8. **RISK** — The changed variables are global (`app/layout.tsx:142`; `app/globals.css:86-88,125-126`), but swap parity covers only `/`, `/services`, and first-viewport elements (`pass-118-tuned-font-fallbacks.md:74-79,97-99`). Consequence: another route or below-fold string can acquire new swap reflow that the loaded-state gates cannot detect.

9. **RISK** — This Windows host has Arial, but the resulting cascade falls through to generic families when `local("Arial")` is unavailable (`app/globals.css:86-88,125-126`), which the brief explicitly parks for Android (`pass-118-tuned-font-fallbacks.md:178-180`). Consequence: the pass does not establish Android CLS parity, and the tuner should at least fail clearly if `"Cand"` did not load from Arial.

10. **PREMISE-OK** — This Next 16.2.6 documents `fallback` as an explicit array and `adjustFontFallback: false` for Google fonts (`font.md:196-220`); the loader returns no metrics when false (`google/loader.js:60-75`) and PostCSS emits real family, optional generated face, then explicit fallbacks (`postcss-next-font.js:63-116`). Consequence: a successful production build yields real family → tuned fallback and omits the generated fallback face; only the development fetch-failure path is an exception (`google/loader.js:149-168`).

11. **PREMISE-OK** — Next computes `"ascent / (unitsPerEm * sizeAdjust)"` and likewise for descent (`node_modules/next/dist/server/font-utils.js:21-33`). Consequence: multiplying the old override by `old_size_adjust / new_size_adjust` correctly preserves absolute vertical metrics.

12. **PREMISE-OK** — The next/font variable declaration is a single-class rule (`postcss-next-font.js:141-152`) applied to `<html>` (`app/layout.tsx:140-145`), so injected `:root:root` wins and propagates through both legacy and CW tokens (`app/globals.css:86-88,125-126`). Consequence: the candidate override reaches normal page typography; only static OG/image surfaces use separate `sans-serif` or literal families (`components/og/cw-og-composition.tsx:42`; `app/icon.svg:7`).

13. **PREMISE-OK** — Repository-wide search found no runtime use of `"Bricolage Grotesque Fallback"` or `"Hanken Grotesk Fallback"` outside the brief/review prompt; consumers only read the variables (`app/globals.css:86-88,125-126`). Consequence: renaming the fallback faces does not break an explicit code dependency.

14. **PREMISE-OK** — The probe applies throttling and disables cache (`perf118a.mjs:144-162`), creates and closes a fresh browser context per load (`perf118a.mjs:246-288`), and the repository registers no service worker. Consequence: localhost can reproduce the delayed swap; there is no evident disk-cache or service-worker path that defeats the bite, and the mandated pre-edit stop handles empirical failure.

15. **PREMISE-OK** — The named gates do not collide: copy-lint excludes CSS/lib fonts (`copy-lint-runner.ts:29-32`), accent lint skips `@font-face` (`accent-states-lint.mjs:125-132`), render-gate reads HTML (`render-gate.mjs:119-130`), type117/layout wait for loaded fonts (`type117.mjs:44-45`; `layout-gate.mjs:329-352`), and card1 checks unrelated text/CSS markers (`card1-115.sh:17-41`). Consequence: these edits should not fail those gates, though those gates also do not close the transient-swap coverage risk above.