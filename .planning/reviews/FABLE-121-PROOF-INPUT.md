# FABLE-121 proof look: can Direction C be made at quality?

Written by the Opus 5 main session, 2026-09-17, for Fable 5.1. One call, about 8 tool calls. Write ONLY
`.planning/reviews/FABLE-121-PROOF.md`. No edits, builds, captures or web research.

## Why this look exists

The operator picked your Direction C on one condition, verbatim: "go with recommendations - but ru able to
produce a quality design? dont want to waste usage if you give me bullshit that we should have another
softwaere do". Before GLM builds the full mock set, a proof was built by GLM (glm-5.3, z.ai quota): the
top of /work and the RFP study's opening band and body, at 390 and 1440, with the six-step flow drawn in
code in the home circle's technique (two overlapping strokes, overshoot, turbulence filter). Round 1 failed
on execution (circle misplaced, labels colliding and clipped; GLM's report said they did not collide, see
LESSONS #36). Round 2 fixed that and measured it. Round 3 fixed the return arrow crossing a label and a
background seam. Opus opened every round's captures. You are the quality gate before the operator sees it.

## Look at

- `.planning/mock/pass-121/proof/work-fold-1440.png`, `work-fold-390.png`, `rfp-study-1440.png`,
  `rfp-study-390.png` (deviceScaleFactor 2; full page).
- For the hand the drawings must match: `.planning/research/pass-121/audit-a/live-home-1440-fold.png` or
  `live-home-1440.png` (the `$20M+` circle).
- Your own spec: `.planning/reviews/FABLE-121-G2.md` sections 3.2, 3.3, 4.1, 4.3, 7 and 10.
- Known deviation from your spec, made by Opus to fix legibility: the flow is two rows of three at every
  size (one viewBox, scale only), not one row at 1440. Rule on it.
- The mock is not the site: the nav is simplified and the grain is an approximation. Judge the design, not
  those.

## Answer, in this order

1. **The hand.** Does the flow read as drawn by a careful human pen in the same hand as the home's circle,
   or as a flowchart tool with a wobble filter, or as clip-art? Name what specifically gives it away either
   way, at 390 and at 1440, on paper and on the dark band.
2. **The punch.** The operator asked for visual punch, animation and design skill on show, and called
   the current page "clean but very plain". Does C as shown deliver that, or is it too quiet (thin lines,
   small drawing, empty boxes, a lot of paper)? If too quiet, name the changes that would make it land
   (drawing scale and placement, stroke weight, what goes inside the boxes, the index rhythm) within the
   ledger and the rules as ruled at G1/G2.
3. **The /work top.** Heading, description, doorway, first entry: right, or what to change.
4. **Verdict**, one of:
   - GO: GLM builds the full mock set per G2 section 7's frame list, with your adjustments listed as exact
     changes (sizes, placements, strings unchanged unless a ledger row allows).
   - ADJUST FIRST: a short list for one more GLM proof round, and what would make it GO.
   - STOP: code-drawn diagrams cannot reach the bar here; the operator sketches the five diagrams by hand
     and they are traced. Say why.
5. **What the operator should hear**, in five plain sentences he can read in the chat.

Plain markdown, no emoji, no em-dashes. Return the verdict line and the five sentences.
