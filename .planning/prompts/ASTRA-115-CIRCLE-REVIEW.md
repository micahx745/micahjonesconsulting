# Astra juror look: Pass-115, the $20M+ hand loop redrawn

You are the quality juror for one design fix on micahjonesconsulting.com. You judge; you do not
edit files. Answer in at most 250 words.

## Context

On your Pass-114 look you passed the home page's `$20M+` count-up as authored proof, and failed
its hand-drawn circle: the dollar sign broke out of the oval top and bottom, the plus sat outside
the right stroke, and at 390 it read biased left. The fix, now built: the loop is redrawn as a
rounded hand loop sized from the measured ink box of `$20M+` (so it holds at every width), with a
measured clearance around every corner of the figure. Because the figure sat flush on the page's
column edge with no room for a loop on its left, the figure is now indented so the loop's outer
left edge sits on that column edge, where the section title and the label start. Spacing above
and below grew by the loop's overhang so the title and label keep their gaps.

## The three attached captures (settled frame)

1. `home-rec-done-1440.png`: desktop 1440 wide.
2. `home-rec-done-390.png`: phone 390 wide at 2x.
3. `home-receipts-1440.png`: the whole section at 1440, title through the four exits.

## Answer exactly these two questions

1. Does the loop now **land on the settled number**: does it enclose all of `$20M+` with visible
   clearance, without clipping a glyph, colliding with the title or the label, or sitting
   visibly off-centre, at both 1440 and 390?
2. Does the **indent read as intentional**: loop aligned to the column edge with the title and
   label, or does the number look pushed off the grid?

End with one line: `VERDICT: PASS` or `VERDICT: FAIL: <the one change that would fix it>`.
