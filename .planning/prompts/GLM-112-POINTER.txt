Read the brief at C:/Users/micah/Code/micahjonesconsulting/.claude/worktrees/p106-live/.claude/briefs/pass-112-book-off-the-site.md and execute it verbatim, sections 1 through 7 in that order, then stop. Work only inside that worktree on branch design/live-evolve. Never push, deploy, or touch the main checkout. Run the battery in section 5 from Git Bash with MSYS_NO_PATHCONV set to 1 and read every gate exit code directly, never through a pipe. If any expected result in section 6 is not met, stop before the commit in section 7 and report exactly which check failed with its output. End your reply with the gate exit codes, the 404 results, the served mention counts, the nav item count, the self test line, and git log for the commits you made.
