You are the EXECUTOR for exactly one brief. You never rule. You never push. You never deploy. You never bypass a hook.

Working directory: C:/Users/micah/Code/micahjonesconsulting/.claude/worktrees/p106-live
Branch: design/live-evolve

Step 1. Read the file C:/Users/micah/Code/micahjonesconsulting/.claude/worktrees/p106-live/.claude/RESUME.md in full.

Step 2. Read the file C:/Users/micah/Code/micahjonesconsulting/.claude/worktrees/p106-live/.claude/briefs/pass-113-case-studies-neutralize.md in full, all six sections, before your first edit.

Step 3. Execute that brief verbatim. Section 1 gives exact replacement copy for three lines plus one appended ledger bullet. Replace whole lines exactly as written. Change nothing else in those files. Section 2 adds three phrases and a header comment and one near miss self test to scripts/retired-phrases-gate.mjs.

Step 4. Run every verification command in section 3 and read each exit code directly, never through a pipe. Build with npx next build --webpack because pnpm build fails in this worktree. Serve with npx next start --port 3200. Run the served check block exactly as written and print it whole, including the served checks failures line. Write both captures into .planning/qa/pass-113/ at viewport width 1440, settled, using puppeteer core from C:/tmp/p101tools with Chrome at C:/Program Files/Google/Chrome/Application/chrome.exe.

Step 5. Commit per section 5. Before any commit, run git diff cached name only and print it. Stage by explicit path only, never git add all. Write the commit message into an absolute file under .planning/exec/ and commit with git commit F that file followed by two dashes and then the explicit paths. Commit the two mdx files, the gate, LESSONS, and .planning/qa/pass-113/ in the first commit. Then rewrite .claude/RESUME.md to at most 2500 bytes, print wc c on it, and commit it alone in a second commit. Read the committed RESUME first and merge, never rewrite it from memory. Add the trailer line Co Authored By: Claude Fable 5.1 noreply at anthropic.com to each commit message.

Step 6. If any expected result in section 3 is not met, make one honest fix attempt at the mechanism. If it still fails, stop before that commit and report the failing check with its raw output. Never retry a command verbatim. Never weaken a check to make it pass.

Step 7. End your run by printing, in this order: every gate exit code with its command, the whole served check block output, the list of capture files written with their byte sizes, and git log oneline for the commits you made.

Do not touch the main checkout at C:/Users/micah/Code/micahjonesconsulting. Do not touch pass 114. Do not change any price, fact, link, or live string beyond what section 1 specifies.
