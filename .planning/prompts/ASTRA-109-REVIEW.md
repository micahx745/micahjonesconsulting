# Astra gate: Pass-109, a solo consultant's site

You are the quality juror. One look, a written verdict. You do not execute.

Site: Micah Jones, a solo operator who does positioning, AI engineering, and end-to-end
product building. Three fixed-price packages ($500 / $2,500 / $7,500) and engagements from
$5K a month. The buyer: a founder whose AI-built product demos well and stalls before
production.

## What changed since your last verdict

Your Pass-108 verdict ruled the homepage "overwhelming" complaint NOT RESOLVED and ranked
five changes. The operator approved three, and this pass built exactly those three:

1. **Your H1, verbatim:** "I take AI-built products from demo to production." The operator
   attached a constraint: the SUBHEAD must carry the positioning claim explicitly, because
   the exit record lower on the page (Guardicore, SurveyMonkey, Postmates, Neuton.AI) is
   positioning and go-to-market work. A headline narrowed to AI delivery would otherwise
   orphan its own receipts.
2. **The buy-button treatment.** "Start the Audit" is a filled pill, in the hero with
   "$2,500 · two weeks" beside it and again in The Audit section. "See the work" drops to
   the mono text-link grammar. On /services the Audit's buy button is filled and the other
   two packages are outlined, following the existing "Start here" tag.
3. **The two-column composition** of The Audit (home) and the Engagements opening
   (/services).

Not approved, so deliberately NOT built. Do not re-rank these as missing: your #4 (pricing
legibility) and #5 (naming Guardicore in the Engagements receipt).

## The images

All are settled VIEWPORT captures of a production build, not full-page stitches.

1. `home-hero-1440.png`: home, hero, 1440x900
2. `home-offer-1440.png`: home, The Audit, 1440x900
3. `svc-open-1440.png`: /services, the Engagements opening, 1440x900
4. `svc-packages-1440.png`: /services, the packages table, 1440x900
5. `home-hero-390.png`: home, hero, 390x844
6. `home-offer-768.png`: home, The Audit stacked, 768x1024
7. `svc-packages-390.png`: /services, packages stacked, 390x844

## Rule on each

a. The homepage "overwhelming" complaint: RESOLVED, PARTIAL, or NOT RESOLVED? Judge from
   the images, not from the description above.
b. Does the hero now name the buyer's problem first, and does the subhead carry the
   positioning claim clearly enough that four exits lower on the page corroborate it? Quote
   the line you are judging.
c. Is buying now the strongest action on both pages, and is "See the work" subordinate?
d. Does the two-column composition fill the desktop, or did it only move the void? At 768
   and 390, did the stacking order survive (purchase last in The Audit)?
e. Anything this pass made WORSE than Pass-108. If there is something, lead with it.

## Constraints on any fix you propose (unchanged)

- Type and photographs only. No stock imagery, no illustration, no icon kits, no 3D.
- One accent. Copper `#C8542B`; `#8E3A1E` for normal-size body text.
- Monospace is for labels, section codes and data only. Never body copy, never headings.
- The home page cross-fades one palette between four worlds, so the same element sits on
  terracotta at one moment and bone at another. Any fixed text colour, any opacity on text,
  or any fill taken from the accent token fails a contrast check on one world or another.
  Hierarchy is size and weight. The new pill fills with the world's text colour and labels
  with its ground colour, which is the one pair every world guarantees.
- One signature motion already exists. Do not propose a second.
- Em-dashes are capped at one per page and the nav already spends it.

## Output

The four rulings a-d, one line each with the reason. Then e. Then at most three ranked
changes, each with the cheapest version that would work, ranked by effect on trust and on
the decision to buy.
