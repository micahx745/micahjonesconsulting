Read the brief at C:/Users/micah/Code/micahjonesconsulting/.claude/worktrees/p106-live/.claude/briefs/pass-111b-services-boxes-and-rail.md in full, including section 13, which overrides earlier sections where they conflict. Execute it verbatim: sections 1 through 7 in order, then the battery in section 7, then the commit in section 9. Work only inside that worktree on branch design/live-evolve. Never push, deploy, or touch the main checkout. Run the battery from Git Bash with MSYS_NO_PATHCONV set to 1 and read every gate exit code directly, never through a pipe. If any expected result in section 8 or any served check in section 13 is not met, stop before the commit and report exactly which check failed with its output. Commit only with explicit paths after printing git diff cached name only. End your reply with every gate exit code, the served check block with its failure count, the seven box heights, the helper check line, the list of PNGs written, and git log for the commits you made.
