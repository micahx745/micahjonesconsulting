Read the brief at C:/Users/micah/Code/micahjonesconsulting/.claude/worktrees/p106-live/.claude/briefs/pass-119-gsap-after-load.md in full. Section 10 overrides earlier sections where they conflict. Execute sections 5.1, 3 with 10.1, 5.2 and 5.3 in that order, then the commit described in section 7. Work only inside that worktree on branch design/live-evolve. Never push, deploy, merge, or touch the main checkout. In Git Bash export MSYS_NO_PATHCONV=1 first. Read every exit code directly, never through a pipe. Never run a build while the server on port 3200 is serving; stop it first with PowerShell Stop-Process on the owning process of port 3200. The scripts chunks119.mjs, reveal119.mjs and lh119-summary.mjs in .planning/exec are the gates; do not edit them. Run reveal119.mjs once on the 5.1 baseline build and record its lines. If any expected result in 5.2 or 5.3 is not met, or any gate prints FAIL, stop before the commit and report the failing lines with their output; never reinterpret an expected value. Save the outputs of chunks119, reveal119 and lh119-summary for before and after into .planning/qa/pass-119. End your reply with: the chunks119 last two lines before and after, every reveal119 line before and after, the full lh119-summary output, the type117, render, axe, layout, card1 and circle115 result lines, the tsc, gate and prettier exit codes, the git diff cached name only output you confirmed before committing, and git log for the commit you made.
