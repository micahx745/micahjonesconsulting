# Harness v2 cross-review, 2026-09-22 (kickoff close step)

Input: `git diff 4dddde3..HEAD -- scripts .claude/hooks .claude/settings.json` at `7c0a4b2` (305,860 bytes, harness
code only; one secret-shaped match, the executor guard's own REDACT_RE pattern, checked before sending). Split in
three so none passed the runner's 200,000-byte cut: A = hooks, settings, wrappers, cross-review (104,510 bytes);
B1 = `scripts/harness` tools (77,938); B2 = `scripts/harness/tests` (123,412).
Legs: `run_cross_review.py --mode diff --legs deepseek,gemini`. Gemini (gemini-2.5-flash): PASS on A, B1 and B2, no
findings. DeepSeek (deepseek-v4-pro): CONCERNS on all three, 13 findings. Codex and GLM: skipped by flag.
Every premise below was checked against the repo before its disposition.

## Group A
- A1 `run_cross_review.py`: `_instruction_for("glmcc")` has no mapping, so the leg breaks. REJECTED: premise false;
  it returns 1,038 characters, and `test_w4_static.py` checks it is non-empty.
- A2 `tier-burn-deny.py`: the Fable count covers the whole loaded transcript, with no arc reset. ACCEPTED AS DESIGNED:
  rule 9 makes a chat one arc; brief C rejected a separate counter file (it drifts); an over-count asks for
  "fable ok" and never blocks silently; `fable-gate.ps1` keeps its own per-arc counter.
- A3 `settings.json`: bash-syntax hook commands on a Windows PowerShell repo. REJECTED: premise false here; Claude
  Code runs hook commands through Git Bash on this machine (the C2 hook's context reached this session at 364K,
  417K and 472K; E1's hooks use the same form).

## Group B1
- B1-1 `status.py`: the lock steal at the deadline can proceed without holding the lock. TRUE, low impact (one block
  can lose an update; status is re-recorded at every `get_usage`). QUEUED: fail the write instead of proceeding.
- B1-2 `visual_qa.py`: the self-test hard-codes a real capture. REJECTED: brief E specifies it (C3.1 check 5), and
  the file is tracked.
- B1-3 `visual-qa.mjs`: the missing-Playwright message versus exit 2. REJECTED: brief E specifies both; the message
  names the command to run on existing captures.
- B1-4 `diff_scope.py`: a git error printed `PASS diff scope: 0 changed paths`. TRUE (probed: a bad `--base` passed).
  FIXED: a git error or a missing brief now prints `FAIL diff scope: ...` and exits 1.
- B1-5 `visual_qa.py`: overflow rows never match a frame by name (`home-390` against `home-390-0`). TRUE, cosmetic
  (overflow is still reported, as its own entries). QUEUED.

## Group B2
- B2-1 `_fixtures.run_hook`: unparseable stdout reads as allow. TRUE, low (every hook's deny cases run the same code
  paths). QUEUED: an `error` decision for non-empty stdout that does not parse.
- B2-2 `run_all.py --live`: 600 s per file against `live_w4_xreview.py`'s 1,800 s child. TRUE. FIXED: live files get
  2,000 s.
- B2-3 `test_e4_routing.py`: hashes pinned to the live AI_ROUTING.md and CLAUDE.md flanks would fail `run_all` at the
  first later edit of either. TRUE. FIXED: cases 3 and 4 read the splice commit `567e719` (its raw blobs reproduce all
  three hashes), still PASS with an edited CLAUDE.md (probed), and print a NOTE if the commit is gone.
- B2-4 Windows-only tests (`claude.cmd`, PowerShell). REJECTED: the harness is machine-local by design (the common
  brief's pre-flight pins PowerShell 5.1 and Python 3.14); no Linux CI runs it.
- B2-5 PIL imported without a declared dependency. REJECTED as a change: the common brief's pre-flight records PIL
  and numpy on this machine; installs are the operator's call.

LEGS: fable=0 astra=0 dspro=3 dsflash=0 gemini=3 sol=0 glm=0 sonnet=0 opus=0
