# Prompt — session opener (use at the start of EVERY session)

Read SPEC.md and the invariants file (CLAUDE.md / rules file) before
anything else. Then, for today's task:

<the task>

Frame the work against the NOW milestone. If anything you want to
propose touches the NOT list, say so and stop instead of building it.
